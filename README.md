# ✈️ Flight Delay Prediction App

A Streamlit web app to predict flight delays using XGBoost.

## 🚀 Setup

1. Clone repository:
   ```bash
   git clone https://github.com/username/flight-delay-app.git
   cd flight-delay-app
